define(["react", "react-dom", "jquery", "Backbone", "classnames"],
  function(React, ReactDOM, $, Backbone, classnames) {
    "use strict";
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.classNames = classnames;
    window.$ = $;
    window.Backbone = Backbone;
});
