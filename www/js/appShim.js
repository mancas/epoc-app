define(["react", "jquery", "Backbone", "classnames", "_", "utils/dateTime"],
  function(React, $, Backbone, classnames, _, DateTimeHelper) {
    "use strict";
    window.React = React;
    window.classNames = classnames;
    window._ = _;
    window.$ = $;
    window.Backbone = Backbone;
    window.DateTimeHelper = DateTimeHelper;
});
